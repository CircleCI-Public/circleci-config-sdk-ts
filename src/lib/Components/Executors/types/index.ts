import * as docker from './DockerExecutor.types';
import * as executor from './Executor.types';
import * as machine from './DockerExecutor.types';
import * as macos from './DockerExecutor.types';
import * as windows from './DockerExecutor.types';
import * as reusable from './ReusableExecutor.types';

export default { docker, machine, macos, windows, executor, reusable };
